---
dependency:
  name: galaxy
  options:
    role-file: "provisioning/requirements.yml"
    ignore-errors: true
provisioner:
  name: ansible
  lint:
    name: ansible-lint
  playbooks:
    prepare: "molecule/resources/prepare.yml"
